# 🤝 Contributing to VOICEBOX

Спасибо за интерес к проекту VOICEBOX! Мы приветствуем любой вклад в развитие проекта.

## 📋 Содержание

- [Code of Conduct](#code-of-conduct)
- [Как помочь проекту](#как-помочь-проекту)
- [Процесс разработки](#процесс-разработки)
- [Стиль кода](#стиль-кода)
- [Commit Guidelines](#commit-guidelines)
- [Pull Request Process](#pull-request-process)

---

## Code of Conduct

Участвуя в проекте, вы соглашаетесь соблюдать наш Code of Conduct:

- 🤝 Быть уважительным и дружелюбным
- 💬 Конструктивно обсуждать идеи
- 🎯 Фокусироваться на том, что лучше для проекта
- 🌍 Приветствовать разнообразие мнений

---

## Как помочь проекту

### 🐛 Сообщить об ошибке

Если вы нашли ошибку:

1. Проверьте [Issues](https://github.com/yourusername/voicebox/issues), может она уже есть
2. Создайте новый Issue со следующей информацией:
   - Описание проблемы
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Версия Python, ОС
   - Скриншоты (если применимо)

**Шаблон для Bug Report:**

```markdown
## Описание
Краткое описание проблемы

## Шаги для воспроизведения
1. Запустить...
2. Нажать на...
3. Видим ошибку...

## Ожидаемое поведение
Что должно было произойти

## Фактическое поведение
Что произошло на самом деле

## Окружение
- ОС: Windows 10
- Python: 3.10.5
- VOICEBOX: 1.0.0

## Дополнительно
Логи, скриншоты, и т.д.
```

### 💡 Предложить улучшение

Есть идея как сделать VOICEBOX лучше?

1. Создайте Issue с тегом `enhancement`
2. Опишите:
   - Что вы хотите добавить/изменить
   - Почему это полезно
   - Как это должно работать
   - Примеры использования

### 📝 Улучшить документацию

Документация всегда может быть лучше:

- Исправление опечаток
- Добавление примеров
- Улучшение объяснений
- Перевод на другие языки

### 🔧 Написать код

1. Выберите Issue с тегом `good first issue` для начала
2. Напишите в Issue что берете задачу
3. Fork репозитория
4. Создайте ветку для фичи
5. Напишите код
6. Отправьте Pull Request

---

## Процесс разработки

### 1. Fork и Clone

```bash
# Fork репозитория через GitHub UI

# Clone вашего форка
git clone https://github.com/YOUR_USERNAME/voicebox.git
cd voicebox

# Добавить upstream remote
git remote add upstream https://github.com/original/voicebox.git
```

### 2. Создание окружения

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Установить dev зависимости
pip install -r requirements-dev.txt
```

### 3. Создание ветки

```bash
# Получить последние изменения
git checkout main
git pull upstream main

# Создать ветку для фичи
git checkout -b feature/amazing-feature

# Или для исправления
git checkout -b fix/bug-description
```

### 4. Разработка

```bash
# Внести изменения
# Писать тесты
# Проверить код

# Запустить тесты
python -m pytest

# Проверить стиль кода
flake8 .
black --check .

# Форматировать код
black .
```

### 5. Commit

```bash
# Добавить изменения
git add .

# Commit с описательным сообщением
git commit -m "feat: add amazing feature"

# Push в ваш fork
git push origin feature/amazing-feature
```

### 6. Pull Request

1. Перейдите на GitHub к вашему форку
2. Нажмите "New Pull Request"
3. Заполните описание по шаблону
4. Дождитесь ревью

---

## Стиль кода

### Python Style Guide

Мы следуем [PEP 8](https://pep8.org/) с небольшими изменениями:

```python
# ═══════════════════════════════════════════════════════════════════
# ХОРОШИЙ КОД
# ═══════════════════════════════════════════════════════════════════

# 1. Документация для всех функций
def transcribe_audio(audio_path: str, language: str = "auto") -> dict:
    """
    Транскрибация аудио файла
    
    Args:
        audio_path: Путь к аудио файлу
        language: Язык аудио (auto для автоопределения)
    
    Returns:
        Словарь с результатами транскрибации
    
    Raises:
        FileNotFoundError: Если файл не найден
        ValueError: Если формат не поддерживается
    """
    pass

# 2. Type hints
from typing import Optional, List, Dict, Any

def process_files(
    files: List[str],
    output_dir: Optional[str] = None
) -> Dict[str, Any]:
    pass

# 3. Константы ЗАГЛАВНЫМИ
MAX_FILE_SIZE = 500 * 1024 * 1024  # 500 MB
SUPPORTED_FORMATS = ['.mp3', '.wav', '.m4a']

# 4. Понятные имена
# Хорошо
transcription_result = transcribe(audio_path)
# Плохо
res = t(ap)

# 5. Docstrings для классов
class Transcriber:
    """
    Класс для транскрибации аудио с использованием Whisper
    
    Attributes:
        model_size: Размер модели ('tiny', 'base', 'small', 'medium', 'large')
        device: Устройство для вычислений ('cpu' или 'cuda')
    
    Example:
        >>> transcriber = Transcriber(model_size='medium')
        >>> result = transcriber.transcribe('audio.mp3')
    """
    pass

# 6. Комментарии для сложной логики
# Вычисляем оптимальный размер batch на основе доступной памяти
available_memory = get_available_memory()
batch_size = min(32, available_memory // (model_size * 2))
```

### Форматирование

```bash
# Автоформатирование с Black
black .

# Проверка импортов с isort
isort .

# Проверка типов с mypy
mypy .
```

### Именование

```python
# Модули и пакеты: lowercase_with_underscores
transcriber.py
audio_processor.py

# Классы: CapitalizedWords
class AudioTranscriber:
    pass

# Функции и методы: lowercase_with_underscores
def transcribe_audio():
    pass

# Константы: UPPERCASE_WITH_UNDERSCORES
MAX_FILE_SIZE = 1000

# Приватные: _leading_underscore
def _internal_helper():
    pass
```

---

## Commit Guidelines

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

### Формат

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Types

- `feat`: Новая функция
- `fix`: Исправление ошибки
- `docs`: Изменения в документации
- `style`: Форматирование, пропущенные точки с запятой и т.д.
- `refactor`: Рефакторинг кода
- `test`: Добавление тестов
- `chore`: Обновление задач сборки, конфигов и т.д.

### Примеры

```bash
# Новая функция
git commit -m "feat(transcriber): add batch processing support"

# Исправление ошибки
git commit -m "fix(gui): resolve window freezing on large files"

# Документация
git commit -m "docs(readme): add installation instructions for macOS"

# Рефакторинг
git commit -m "refactor(config): simplify configuration loading"

# Breaking change
git commit -m "feat(api): change return format

BREAKING CHANGE: transcribe() now returns dict instead of string"
```

---

## Pull Request Process

### Checklist перед PR

- [ ] Код соответствует style guide
- [ ] Все тесты проходят
- [ ] Добавлены новые тесты (если применимо)
- [ ] Документация обновлена
- [ ] Changelog обновлен
- [ ] Коммиты следуют Conventional Commits
- [ ] PR имеет понятное описание

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix (исправление ошибки)
- [ ] New feature (новая функция)
- [ ] Breaking change (изменение API)
- [ ] Documentation update (обновление документации)

## Связанные Issues
Closes #123
Relates to #456

## Как тестировалось
Опишите как вы тестировали изменения

## Скриншоты (если применимо)
![Screenshot](url)

## Checklist
- [ ] Код следует style guide
- [ ] Self-review выполнен
- [ ] Добавлены комментарии в сложных местах
- [ ] Документация обновлена
- [ ] Нет новых warnings
- [ ] Добавлены тесты
- [ ] Все тесты проходят
```

### Процесс ревью

1. Автоматические проверки должны пройти
2. Минимум 1 approval от мейнтейнеров
3. Все комментарии разрешены
4. Конфликты разрешены
5. Merge в main

---

## Структура проекта

```
voicebox/
├── launcher_gui.py           # Главный лаунчер
├── gui_mega.py               # MEGA интерфейс
├── transcriber.py            # Модуль транскрибации
├── config.py                 # Конфигурация
├── utils.py                  # Утилиты
│
├── tests/                    # Тесты
│   ├── test_transcriber.py
│   ├── test_config.py
│   └── ...
│
├── docs/                     # Документация
│   ├── installation.md
│   ├── usage.md
│   └── ...
│
└── examples/                 # Примеры
    ├── basic_usage.py
    └── ...
```

---

## Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=. --cov-report=html

# Конкретный файл
pytest tests/test_transcriber.py

# Конкретный тест
pytest tests/test_transcriber.py::test_transcribe_audio
```

### Написание тестов

```python
import pytest
from transcriber import Transcriber

class TestTranscriber:
    """Тесты для Transcriber"""
    
    @pytest.fixture
    def transcriber(self):
        """Fixture для создания transcriber"""
        return Transcriber(model_size='tiny')
    
    def test_transcribe_audio(self, transcriber, tmp_path):
        """Тест транскрибации аудио"""
        # Arrange
        audio_file = tmp_path / "test.mp3"
        create_test_audio(audio_file)
        
        # Act
        result = transcriber.transcribe(str(audio_file))
        
        # Assert
        assert result is not None
        assert 'text' in result
        assert len(result['text']) > 0
```

---

## Контакты

- **GitHub Issues:** https://github.com/yourusername/voicebox/issues
- **GitHub Discussions:** https://github.com/yourusername/voicebox/discussions
- **Email:** voicebox@example.com

---

## Лицензия

Внося вклад в VOICEBOX, вы соглашаетесь что ваш код будет распространяться под лицензией [MIT License](LICENSE).

---

## Благодарности

Спасибо всем кто вносит вклад в проект! 🎉

Ваше имя будет добавлено в [Contributors](https://github.com/yourusername/voicebox/graphs/contributors).

---

**Happy Coding! 🚀**
